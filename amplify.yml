version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required for this static prototype"
    build:
      commands:
        - echo "Publishing static site"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
