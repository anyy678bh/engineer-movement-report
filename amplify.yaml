version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required"
    build:
      commands:
        - echo "Static site ready"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
