AWSTemplateFormatVersion: '2010-09-09'
Description: S3 bucket with only OwnershipControls
Resources:
  UserProfileImagesBucket:
    Type: AWS::S3::Bucket
    Properties:
      OwnershipControls:
        Rules:
        - ObjectOwnership: BucketOwnerEnforced
